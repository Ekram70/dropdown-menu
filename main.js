let dropdownListItem = document.querySelectorAll(".dropdown-list-item");
console.log(dropdownListItem);

let dropdownSubmenuItem = document.querySelectorAll(".dropdown-submenu-item");
console.log(dropdownSubmenuItem);

for (let x = 0; x < dropdownListItem.length; x++) {
  dropdownListItem[0].addEventListener("click", function () {});
}
